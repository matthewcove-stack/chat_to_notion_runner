# Codex Rules — action_relay

- Keep the canonical workflow docker-only; Windows wrapper is optional convenience.
- Do not bake secrets into examples.
- Examples must validate against notion_assistant_contracts schemas.
