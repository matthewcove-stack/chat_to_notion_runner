services:
  action-client:
    build:
      context: ./client
    env_file:
      - .env

  test:
    build:
      context: .
      dockerfile: tests/Dockerfile
    env_file:
      - .env
    environment:
      DRY_RUN: "1"